import "fmt";

func add(a, b) {
  return a + b;
}

func multiply(x, y) {
  let result = x * y;
  return result;
}

func greet(name) {
  fmt.println("Hello, " + name + "!");
}

x = 10;
y = 20;
result = add(x, y);
fmt.println("Result: " + result); 